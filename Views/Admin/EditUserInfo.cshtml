@model Practical_test_application.Models.User_view_model
@{
    ViewBag.Title = "EditUserInfo";
}

<h2>EditUserInfo</h2>

<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<script src="~/Scripts/bootstrap.min.js"></script>

<div class="container" style="width:40%;margin-top:2%">
    <table>
        <tr>
            <th>Name</th>
            <th>Phone</th>
            <th>Email</th>
            <th>Password</th>
            <th>Action</th>
        </tr>
        <tbody>
            @if (ViewBag.UserList != null)
            {
                foreach (var item in ViewBag.UserList)
                {
                    <tr id="row_@item.ID">
                        <td>@item.Name</td>
                        <td>@item.Phone</td>
                        <td>@item.Email</td>
                        <td>@item.Password</td>
                        <td><a href="#" class="btn btn-info" onclick="AddEditUser(@item.ID)">Edit</a></td>

                        <td>
                            <a href="#" class="btn btn-danger"
                               onclick="ConfirmDelete(@item.ID)">Delete </a>
                        </td>
                    </tr>
                }
            }
        </tbody>
    </table>
    <a href="#" class="btn btn-primary" onclick="AddEditUser(0)">New</a>

    <div class="modal fade" id="myModal1">
        <div class="modal-dialog">
            <div class="modal-content">

                <div class="modal-header">
                    <h3 class="modal-title">Edit User Details</h3>

                    <button type="button" class="btn-close" data-dismiss="modal" id="btnHideModal1"></button>
                    <br />

                </div>
                <div class="modal-body" id="myModalBodyDiv1">

                </div>

            </div>

        </div>

    </div>
    <div class="modal fade" id="myModal">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <a href="#" class="close" data-dismiss="modal">&times;</a>
                    <h3 class="modal-title">Delete Employee</h3>
                </div>
                <div class="modal-body">
                    <h4>Are you sure ? You want to delete this. </h4>
                    <div style="text-align:center;display:none" id="loaderDiv">
                        <img src="~/Content/InternetSlowdown_Day.gif" width="150" />
                    </div>
                </div>
                <div class="modal-footer">
                    <a href="#" class="btn btn-default" data-dismiss="modal">Cancel</a>
                    <a href="#" class="btn btn-success" onclick="DeleteEmployee()">Confirm</a>
                </div>
            </div>
        </div>
    </div>
    @*hidden field for storing current employeeId*@

    <input type="hidden" id="hiddenEmployeeId" />
</div>


<script type="text/javascript">
    $(document).ready(function () {
        $("#btnShowModal").click(function () {
            $("#myModal1").modal('show');
        });
        $("#btnHideModal").click(function () {
            $("#myModal1").modal('hide');
        });
        $("#btnHideModal1").click(function () {
            $("#myModal1").modal('hide');
        });
    });
</script>

<script>
    debugger
    var AddEditUser = function (Id) {
        var url = "/Admin/AddEditUser?Id=" + Id;
        $("#myModalBodyDiv1").load(url, function () {
            $("#myModal1").modal("show");
        })
    }
</script>

<script>
    var ConfirmDelete = function (Id) {
        debugger
        $("#hiddenEmployeeId").val(Id);
        $("#myModal").modal('show');
    }
    var DeleteEmployee = function () {
        debugger
        $("#loaderDiv").show();
        var user_id = $("#hiddenEmployeeId").val();
        $.ajax({
            type: "POST",
            url: "/Admin/DeleteEmployee",
            data: { Id: user_id },
            success: function (result) {
                $("#loaderDiv").hide();
                $("#myModal").modal("hide");
                $("#row_" + empId).remove();
            }
        })
    }
</script>

