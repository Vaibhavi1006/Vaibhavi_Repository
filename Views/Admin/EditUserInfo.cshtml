@model Practical_test_application.Models.User_view_model
@{
    ViewBag.Title = "EditUserInfo";
    Layout = null;
}


<script src="~/Scripts/jquery-3.4.1.min.js"></script>
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<script src="~/Scripts/bootstrap.min.js"></script>



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <style>

        table, td {
            border: 1px solid #b7b6b6;
            padding: 10px;
            background-color: white;
            align-items: center;
            text-align: center;
            border-collapse: collapse;
        }

        th {
            border: 1px solid #b7b6b6;
            padding-left: 10px;
            padding-right: 10px;
            background-color: #0dcaf0;
            color: #ffffff;
            padding: 10px;
            align-items: center;
            text-align: center;
        }

        .center {
            margin-left: auto;
            margin-right: auto;
            width: 80%;
            margin-bottom: 30px;
        }

        h2 {
        }
    </style>

</head>
<body>
    <div>
        <h2 style="text-align: center; color: #7d7878; padding-top: 10px;">Edit User Information</h2>
        <div class="container">
            <div class="center">

                <a href="#" class="btn btn-info" style=" color:#FFFFFF; text-align:center;" onclick="AddEditUser(0)"> Add New</a>
            </div>
        </div>

        <div class="container" style="width:100%;margin-top:2%">
            <table class="center">
                <tr>
                    <th>Name</th>
                    <th>Phone</th>
                    <th>Email</th>
                    <th>Password</th>
                    <th>Action</th>
                    <th>Action</th>
                </tr>
                <tbody>
                    @if (ViewBag.UserList != null)
                    {
                        foreach (var item in ViewBag.UserList)
                        {
                            <tr id="row_@item.ID">
                                <td>@item.Name</td>
                                <td>@item.Phone</td>
                                <td>@item.Email</td>
                                <td>@item.Password</td>
                                <td><a href="#" class="btn btn-info" style="color:#ffffff;" onclick="AddEditUser(@item.ID)">Edit</a></td>

                                <td>
                                    <a href="#" class="btn btn-danger"
                                       onclick="ConfirmDelete(@item.ID)">Delete </a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>

            <div class="modal fade" id="myModal1">
                <div class="modal-dialog">
                    <div class="modal-content">

                        <div class="modal-header">
                            <h3 class="modal-title">Edit User Details</h3>

                            <button type="button" class="btn-close" data-dismiss="modal" id="btnHideModal1"></button>
                            <br />

                        </div>
                        <div class="modal-body" id="myModalBodyDiv1">

                        </div>

                    </div>

                </div>

            </div>
            <div class="modal fade" id="myModal">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h3 class="modal-title">Delete User</h3>
                            <a href="#" class="close" data-dismiss="modal">&times;</a>

                        </div>
                        <div class="modal-body">
                            <h4>Are you sure ? You want to delete this </h4>
                            <div style="text-align:center;display:none" id="loaderDiv">
                                <img src="~/Content/InternetSlowdown_Day.gif" width="150" />
                            </div>
                        </div>
                        <div class="modal-footer">
                            <a href="#" class="btn btn-default" data-dismiss="modal">Cancel</a>
                            <a href="#" class="btn btn-success" onclick="DeleteEmployee()">Confirm</a>
                        </div>
                    </div>
                </div>
            </div>
            @*hidden field for storing current employeeId*@

            <input type="hidden" id="hiddenEmployeeId" />
        </div>
    </div>
</body>
</html>


<script type="text/javascript">
    $(document).ready(function () {
        $("#btnShowModal").click(function () {
            $("#myModal1").modal('show');
        });
        $("#btnHideModal").click(function () {
            $("#myModal1").modal('hide');
        });
        $("#btnHideModal1").click(function () {
            $("#myModal1").modal('hide');
        });
    });
</script>

<script>
    debugger
    var AddEditUser = function (Id) {
        var url = "/Admin/AddEditUser?Id=" + Id;
        $("#myModalBodyDiv1").load(url, function () {
            $("#myModal1").modal("show");
        })
    }
</script>

<script>
    var ConfirmDelete = function (Id) {
        debugger
        $("#hiddenEmployeeId").val(Id);
        $("#myModal").modal('show');
    }
    var DeleteEmployee = function () {
        debugger
        $("#loaderDiv").show();
        var user_id = $("#hiddenEmployeeId").val();
        $.ajax({
            type: "POST",
            url: "/Admin/DeleteEmployee",
            data: { Id: user_id },
            success: function (result) {
                $("#loaderDiv").hide();
                $("#myModal").modal("hide");
                $("#row_" + empId).remove();
            }
        })
    }
</script>

